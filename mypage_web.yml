name: MyPage::Web
default_view: 'TT'
encoding: UTF-8
View::TT:
  INCLUDE_PATH:
    - template
  TEMPLATE_EXTENSION: '.tt'
  ENCODING : utf-8
View::JSON:
  # allow_callback: 0
  # callback_param: 'cb'
  # expose_stash: ''
  # json_driver: JSON
  # no_x_json_header: 0
Model::DBIC:
  connect_info:
    - 'dbi:mysql:mypage'
    - 'root'
    - ''
    - mysql_enable_utf8 : 1
