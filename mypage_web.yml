name: MyPage::Web
default_view: 'TT'
encoding: UTF-8
View::TT:
  INCLUDE_PATH:
    - template
  TEMPLATE_EXTENSION: '.tt'
  ENCODING : utf-8
View::JSON:
  # allow_callback: 0
  # callback_param: 'cb'
  expose_stash:
    - address_book
    - address_books
    - prefectures
  # json_driver: JSON
  # no_x_json_header: 0
Model::DBIC:
  schema_class: 'MyPage::Web::Schema'
  connect_info:
    - 'dbi:mysql:mypage'
    - 'root'
    - ''
    - AutoCommit : 1
      on_connect_do : ["SET NAMES utf8"]
      mysql_enable_utf8 : 1
  # connect_info:
    # dsn: 'dbi:mysql:mypage'
    # user: 'root'
    # password: ''
    # AutoCommit : 1
    # on_connect_do : ["SET NAMES utf8"]
    # mysql_enable_utf8 : 1
